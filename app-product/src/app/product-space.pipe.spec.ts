import { ProductSpacePipe } from './product-space.pipe';

describe('ProductSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
